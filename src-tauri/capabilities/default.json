{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum privileges for the main window. Notably absent: the shell plugin — the webview cannot execute processes, only call named Rust commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "dialog:allow-open"
  ]
}
